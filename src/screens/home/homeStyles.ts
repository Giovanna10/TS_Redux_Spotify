import { StyleSheet } from "react-native";

const homeStyles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: '15%'
  },
  userName: {
    color: "#fefefe"
  }
});

export default homeStyles
